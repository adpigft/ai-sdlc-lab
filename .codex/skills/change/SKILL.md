---
name: change
description: Temporary compatibility alias for the canonical `change-request` skill during the migration window.
---

# Change Alias

This alias is retained for one migration cycle.

Use `$change-request` for the canonical change-request workflow.

