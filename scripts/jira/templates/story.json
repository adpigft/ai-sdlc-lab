{
  "issueType": "Story",
  "externalId": "{{external_id}}",
  "parentExternalId": "{{parent_external_id}}",
  "summary": "{{summary}}",
  "description": "{{description}}",
  "labels": ["ai-sdlc", "feature", "{{capability_id}}", "{{feature_id}}"],
  "fields": {
    "domain": "{{domain}}",
    "capability": "{{capability}}",
    "feature": "{{feature}}",
    "mappedRequirementIds": "{{mapped_requirement_ids}}",
    "acceptanceScenarioIds": "{{acceptance_scenario_ids}}",
    "linkedImplementationSliceIds": "{{linked_implementation_slice_ids}}",
    "sourceArtifacts": "{{source_artifacts}}"
  }
}
